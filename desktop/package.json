{
  "name": "utm-thesis-converter-desktop",
  "version": "1.0.0",
  "description": "Standalone desktop application to convert Word thesis documents to LaTeX format",
  "main": "public/electron.js",
  "homepage": "./",
  "author": "Omar Lafta",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/omarlafta82-cloud/Word-to-latex-thesis-addin.git"
  },
  "scripts": {
    "react-start": "react-scripts start",
    "react-build": "react-scripts build",
    "build": "npm run react-build && electron-builder",
    "build-win": "npm run react-build && electron-builder --win --publish never -c.win.signAndEditExecutable=false",
    "build-mac": "npm run react-build && electron-builder --mac",
    "build-linux": "npm run react-build && electron-builder --linux",
    "dev": "concurrently \"npm run react-start\" \"wait-on http://localhost:3000 && electron .\"",
    "electron-dev": "electron .",
    "cli": "node src/cli/index.js",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-icons": "^4.11.0",
    "mammoth": "^1.6.0",
    "yargs": "^17.7.0",
    "chalk": "^4.1.2",
    "inquirer": "^8.2.5",
    "electron-squirrel-startup": "^1.0.1",
    "electron-updater": "^5.3.0",
    "electron-is-dev": "^1.2.0"
  },
  "devDependencies": {
    "react-scripts": "5.0.1",
    "@types/react": "^18.0.0",
    "@types/react-dom": "^18.0.0",
    "@types/node": "^18.0.0",
    "electron": "^28.3.0",
    "electron-builder": "^24.13.3",
    "concurrently": "^8.0.0",
    "wait-on": "^7.0.0"
  },
  "browserslist": {
    "production": [
      "last 1 electron version"
    ],
    "development": [
      "last 1 electron version"
    ]
  },
  "build": {
    "appId": "com.utm-thesis-converter.app",
    "productName": "UTM Thesis Converter",
    "files": [
      "build/**/*",
      "public/electron.js",
      "public/preload.js",
      "src/**/*"
    ],
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": ["x64"]
        },
        {
          "target": "portable",
          "arch": ["x64"]
        }
      ]
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "installerIcon": null,
      "uninstallerIcon": null
    },
    "mac": {
      "target": [
        "dmg",
        "zip"
      ],
      "category": "public.app-category.utilities"
    },
    "linux": {
      "target": [
        "AppImage",
        "deb"
      ],
      "category": "Utility"
    }
  }
}
